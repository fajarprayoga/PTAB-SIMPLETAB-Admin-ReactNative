import React from 'react'
import { View} from 'react-native'

const Distance = (props) => {
    return (
        <View style={{marginVertical : props.distanceV, marginHorizontal :props.distanceH}} />
    )
}
export default Distance
